# Проверки NutriFit 2.3

21 сентября 2026, JBR 21, compile SDK 36, эмулятор Pixel 10 Pro API 37, отдельный пакет `com.nutrifit.app.qa`.

- `assembleDebug assembleQa testDebugUnitTest lintDebug connectedQaAndroidTest` — успешно.
- 17 модульных тестов: расчёты, правила аккаунта, хеширование, поиск и преобразование порций, словари исторических названий.
- 9 Android-тестов: импорт каталога, миграции схем 1→3 и 2→3, сохранность аккаунта/дневника/веса/избранного/планов/воды/покупок/прогресса, переключение RU/EN/PL, отдельная демо-база, кэш API-переводов и откат некорректной транзакции.
- `python project/check_database.py`: целостность и внешние ключи SQLite, 120 рецептов, 100 продуктов, 14 уроков, 12 категорий, порядок ингредиентов/шагов, калории, аллергены и полнота встроенных переводов.
- Перед удалением JSON сравнили исходные числа, ингредиенты, шаги и словари с новой базой. Резервный архив проверен побайтно.
- `python project/check_translations.py`: по 243 ресурса интерфейса для EN и PL, совпадение форматных параметров.
- Lint: 0 ошибок, 22 предупреждения. Они включают версии зависимостей, неиспользуемые ресурсы и layout диалогов; отчёт находится в `app/build/reports/lint-results-debug.html`.

Ручная проверка API: в QA-приложении скачаны модели, выполнены реальные переводы 302 текстов RU→EN и 302 текстов RU→PL. После остановки процесса из файлов SQLite прочитаны 302 строки соответствующего языкового кэша; например, API вернул «Banana Oatmeal». Затем Wi-Fi и мобильная сеть эмулятора временно отключены, приложение перезапущено: польские машинные названия открылись из кэша. Сетевые настройки восстановлены. Скриншоты: `translation-2.3.png`, `sqlite-mlkit-offline-2.3.png`.

Машинный польский перевод местами стилистически слабый. Встроенный перевод доступен до подготовки моделей; профессиональная вычитка машинного результата не выполнялась. На физических телефонах, Android 8 и со всеми вариантами сетевых ошибок полный проход не проводился.

Ниже — история проверок старых версий, а не описание текущего формата хранения.

---

# Проверки NutriFit 2.1

21 сентября 2026: `assembleDebug assembleQa testDebugUnitTest lintDebug connectedQaAndroidTest` — успешно на JBR 21, SDK 36 и эмуляторе Pixel 10 Pro, API 37.

- 21 модульный тест: все пройдены. Новый набор проверяет перевод всех рецептов, продуктов, ингредиентов, шагов, уроков и подписей видео; неизменность чисел, ID, URL и исходного JSON; поиск на польском без диакритики; перевод названий дневника между тремя языками.
- 5 инструментальных тестов: все пройдены. Дополнительно проверена смена ru → en → pl → ru с сохранением избранного, плана, отметки съеденного и дневника. Исходная запись рецепта в SQLite не меняется.
- Android Lint: 0 ошибок, 22 предупреждения (обновления библиотек, неиспользуемые ресурсы, оформление диалога).
- Система реальных платежей и внешний Translation API не используются.

На эмуляторе вручную проверены английский каталог, переведённые ингредиенты и шаги рецепта, переключение на польский и польские названия карточек. Скриншоты: `catalog-en-2.1.png`, `recipe-en-2.1.png`, `catalog-pl-2.1.png`. После оптимизации копирования JSON повторно запущены модульные тесты и инструментальная проверка переключения языков.

Предыдущие проверки версии 2.0 приведены ниже как история; ограничения перевода в них устранены в 2.1.

---

# Проверки NutriFit 2.0

Дата: 17 сентября 2026. JBR 21, SDK 36; эмулятор Pixel 10 Pro, Android 17 / API 37. Проверки выполнялись в отдельном пакете com.nutrifit.app.qa.

## Автоматические результаты

- assembleDebug, assembleQa: успешно.
- testDebugUnitTest: 17 тестов, 0 ошибок. Расчёты, пароль, правила аккаунта, 120 уникальных рецептов и 100 продуктов, состав/БЖУ, фильтры, источники видео.
- connectedQaAndroidTest: 4 теста, 0 ошибок. Миграция SQLite сохраняет дневник и вес, каталог не дублируется, повторный перенос плана не создаёт дубль, покупки суммируются, локальные данные сохраняются после открытия базы, смена и восстановление пароля работают.
- check_translations.py: по 226 строк и массивов для английского и польского, полнота переводов и форматные аргументы совпадают.
- lintDebug: 0 ошибок, 22 предупреждения (обновления библиотек, неиспользуемые ресурсы и оформление диалога).

## Проверка экранов

Пройдены регистрация и показ кода восстановления, сохранение профиля, загрузка 120 рецептов, избранное, карточка блюда, добавление в дневник и план, включение демо Plus, перенос съеденного, завершение урока, перезапуск с сохранённым дневником. Проверены польские подписи, дата, перевод названий приёмов пищи в прежних записях и сохранение языка после повторного запуска. Английские ресурсы проверены сборкой и скриптом полноты; полный ручной проход на английском не выполнен.

Скриншоты находятся в screenshots. registration.png сделан до добавления переключателя языка. Внешние видео проверены по страницам и метаданным, а не полным воспроизведением. Локальный видеоплеер собран, но просмотр пользовательского видео не проверялся. Полный проход на физических телефонах, Android 8 и с увеличенным шрифтом не выполнялся. Отсутствие задержек на всех устройствах не гарантируется: SQLite и хеширование вынесены в фон, каталог использует RecyclerView и DiffUtil.

## Повторение

```powershell
.\gradlew.bat assembleDebug assembleQa testDebugUnitTest lintDebug
$env:ANDROID_SERIAL='emulator-5554'
.\gradlew.bat connectedQaAndroidTest '-Pandroid.injected.device.serial=emulator-5554'
python project/check_translations.py
python project/package_project.py
```

Инструментальным тестам нужен подключённый эмулятор. Они используют отдельные случайные имена базы и настроек. Данные аккаунтов и дампы базы не входят в ZIP.
